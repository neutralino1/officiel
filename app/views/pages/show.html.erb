<div id="main-col">
  <h1 class="text-shadow"><%= @page.title %></h1>
  <p><%= @page.content.html_safe %></p>
</div>
<div id="sidebar">
  <%= link_to(content_tag(:div, 'Edit', :class=>'btn blue-btn'), edit_page_path(@page)) %>
</div>
