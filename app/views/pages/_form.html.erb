<%= form_for(@page) do |f| %>
<%= f.text_field :title, :placeholder => 'Page title...' %>
<%= f.text_area :content, :placeholder => 'Page content...'  %>
<% button_txt = @page.new_record? ? 'Create page' : 'Update page' %>
<%= f.submit button_txt, :class => 'blue-button' %>
<% end %>
